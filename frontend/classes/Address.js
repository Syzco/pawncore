export default class Address {
    constructor(addressId, street, street2, city, state, zipcode) {
        this.addressId = addressId;
        this.street = street;
        this.stret2 = street2;
        this.city = city;
        this.state = state;
        this.zipcode = zipcode;        
    }
}