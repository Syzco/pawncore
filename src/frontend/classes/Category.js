export default class Category{
    constructor(categoryId, name, parentId, image) {
        this.categoryId = categoryId;
        this.name = name;
        this.parentId = parentId;
        this.image = image;
    }
}