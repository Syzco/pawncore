export default class PhoneNumber {
    constructor(phoneId, type, number, customerId) {
        this.phoneId = phoneId;
        this.type = type;
        this.number = number;
        this.customerId = customerId;
    }
}