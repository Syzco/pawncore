export default class Catalog {
    constructor(catalogId, brand, model, image, UPC, category) {
        this.catalogId = catalogId;
        this.brand = brand;
        this.model = model;
        this.image = image;
        this.UPC = UPC;
        this.category = category;
    }
}