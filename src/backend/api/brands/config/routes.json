{
  "routes": [
     {
          "method": "GET",
          "path": "/search/brand/:name?",
          "handler": "Brands.search",
          "config": {
               "policies": []
          }
     }
  ]
}
