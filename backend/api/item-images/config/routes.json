{
  "routes": [
    {
      "method": "GET",
      "path": "/item-images",
      "handler": "item-images.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/item-images/count",
      "handler": "item-images.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/item-images/:id",
      "handler": "item-images.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/item-images",
      "handler": "item-images.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/item-images/:id",
      "handler": "item-images.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/item-images/:id",
      "handler": "item-images.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
